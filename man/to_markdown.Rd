% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{to_markdown}
\alias{to_markdown}
\alias{to_markdown_raw}
\title{Convert a document to Markdown}
\usage{
to_markdown(path, format = NULL)

to_markdown_raw(bytes, format = NULL)
}
\arguments{
\item{path}{Path to a single document.}

\item{format}{Format to parse as, or \code{NULL} (the default) to detect it. One
of \code{\link[=anydoc_formats]{anydoc_formats()}}. Several file types share a parser, so this names the
parser rather than the extension: \code{.xlsx}, \code{.xlsb} and \code{.xls} are all
\code{"excel"}.}

\item{bytes}{A raw vector holding a single document.}
}
\value{
A length-1 character vector of Markdown, marked as UTF-8.
}
\description{
\code{to_markdown()} converts a file; \code{to_markdown_raw()} converts a document
already in memory. Both produce GitHub-Flavored Markdown.
}
\details{
The format is normally detected from the document's \emph{contents} rather than
its name, so a mislabelled file still converts correctly. For
\code{to_markdown()}, the file extension is consulted as a fallback; for
\code{to_markdown_raw()} there is no name to fall back on, so a format that
carries no signature must be named explicitly. In practice that means
\strong{\code{format = "csv"} is required for CSV given as raw bytes}.
}
\section{Resource use}{

Conversion uses at most two cores. PDF parsing is internally parallel, and
its thread pool would otherwise grow to the number of logical CPUs; the
package caps it on first use. Set the \code{RAYON_NUM_THREADS} environment
variable before the first conversion to choose a different size.
}

\section{Errors}{

Failures are signalled as conditions of class \code{anydoc_error}, with a more
specific subclass naming the cause, so callers branch on the cause rather
than on message text:

\describe{
\item{\code{anydoc_error_unsupported}}{The format is unknown or cannot be converted.}
\item{\code{anydoc_error_needsOcr}}{A PDF with scanned or image-only pages.
\code{anydoc} does not perform OCR, and returns nothing rather than silently
omitting those pages. The condition carries \code{pages} (an integer vector of
the 1-indexed pages needing OCR) and \code{page_count}.}
\item{\code{anydoc_error_malformed}}{The document is structurally unusable.}
\item{\code{anydoc_error_encrypted}}{The document is password-protected.}
\item{\code{anydoc_error_resourceLimit}}{A fixed safety limit was exceeded.}
\item{\code{anydoc_error_missingPart}}{A part required for any output is absent.}
\item{\code{anydoc_error_io}}{The input could not be read.}
\item{\code{anydoc_error_panic}}{The Rust library panicked. The condition
message carries the panic message; the panic is caught at the C boundary
rather than being allowed to unwind into R. Report these upstream.}
}
}

\examples{
csv <- tempfile(fileext = ".csv")
write.csv(head(mtcars[1:3], 3), csv)
cat(to_markdown(csv))

# Raw bytes have no extension to fall back on, so CSV must be named.
cat(to_markdown_raw(charToRaw("a,b\n1,2\n"), format = "csv"))

}
